{
  "schemaVersion": 2,
  "id": "percentage-newtype-001",
  "arcId": "table-driven-domain-tests",
  "arcTitle": "Table-driven domain tests",
  "order": 85,
  "day": 1,
  "arcLength": 6,
  "title": "Define a bounded Percentage type",
  "conceptId": "percentage-newtype",
  "difficulty": "easy",
  "estimatedMinutes": 6,
  "scenario": "A percentage should not be represented as any arbitrary integer throughout the codebase.",
  "instructions": "Define Percentage, PercentageError, value, and TryFrom<u16> that rejects values above 100.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "// TODO: define Percentage with a TryFrom<u16> boundary.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use rust_daily_lesson::{Percentage, PercentageError};\n\n#[test]\nfn percentage_accepts_values_from_zero_to_hundred() {\n    assert_eq!(Percentage::try_from(0).map(|value| value.value()), Ok(0));\n    assert_eq!(Percentage::try_from(100).map(|value| value.value()), Ok(100));\n}\n\n#[test]\nfn percentage_rejects_out_of_range_values() {\n    assert_eq!(Percentage::try_from(101), Err(PercentageError::OutOfRange));\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Define Percentage as a private u8 newtype and PercentageError with an OutOfRange variant."
    },
    {
      "level": 2,
      "body": "Add value(&self) -> u8 and implement TryFrom<u16> so values above 100 return PercentageError::OutOfRange."
    },
    {
      "level": 3,
      "body": "One complete solution for Define a bounded Percentage type is shown below.",
      "solutionCode": "#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct Percentage(u8);\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum PercentageError {\n    OutOfRange,\n}\n\nimpl Percentage {\n    pub fn value(&self) -> u8 {\n        self.0\n    }\n}\n\nimpl TryFrom<u16> for Percentage {\n    type Error = PercentageError;\n\n    fn try_from(value: u16) -> Result<Self, Self::Error> {\n        if value > 100 {\n            return Err(PercentageError::OutOfRange);\n        }\n\n        Ok(Self(value as u8))\n    }\n}"
    }
  ],
  "completionExplanation": "Percentage now enforces the domain boundary at construction time and exposes the stored value through a small accessor.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "tuple_struct_fields",
            "structName": "Percentage",
            "requiredTypes": [
              "u8"
            ]
          },
          {
            "type": "impl_trait_for_type",
            "traitName": "TryFrom<u16>",
            "typeName": "Percentage"
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std"
      }
    ]
  }
}
