{
  "schemaVersion": 2,
  "id": "config-methods-003",
  "arcId": "config-service",
  "arcTitle": "Configure a small service",
  "order": 11,
  "day": 3,
  "arcLength": 6,
  "title": "Add a small Config setter",
  "conceptId": "methods-basic",
  "difficulty": "easy",
  "estimatedMinutes": 7,
  "scenario": "Callers should be able to start from Config::default and customize one field without reaching into every detail.",
  "instructions": "Add Config::with_service_url(self, service_url: impl Into<String>) -> Self. Update the field and return self.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "pub struct Config {\n    pub service_url: String,\n    pub max_connections: usize,\n    pub use_tls: bool,\n}\n\nimpl Default for Config {\n    fn default() -> Self {\n        Self {\n            service_url: \"http://localhost:8080\".to_owned(),\n            max_connections: 32,\n            use_tls: false,\n        }\n    }\n}\n\n// Continue from the previous lesson.\n// TODO: add with_service_url here.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use rust_daily_lesson::Config;\n\n#[test]\nfn with_service_url_updates_only_the_url() {\n    let config = Config {\n        service_url: \"http://localhost:8080\".to_owned(),\n        max_connections: 32,\n        use_tls: false,\n    }\n    .with_service_url(\"https://api.example.com\");\n\n    assert_eq!(config.service_url, \"https://api.example.com\");\n    assert_eq!(config.max_connections, 32);\n    assert!(!config.use_tls);\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "A consuming setter can take mut self, change a field, and return self."
    },
    {
      "level": 2,
      "body": "impl Into<String> lets callers pass either String or &str."
    },
    {
      "level": 3,
      "body": "Returning Self keeps the method chain-friendly.",
      "solutionCode": "pub struct Config {\n    pub service_url: String,\n    pub max_connections: usize,\n    pub use_tls: bool,\n}\n\nimpl Default for Config {\n    fn default() -> Self {\n        Self {\n            service_url: \"http://localhost:8080\".to_owned(),\n            max_connections: 32,\n            use_tls: false,\n        }\n    }\n}\n\nimpl Config {\n    pub fn with_service_url(mut self, service_url: impl Into<String>) -> Self {\n        self.service_url = service_url.into();\n        self\n    }\n}"
    }
  ],
  "completionExplanation": "A small method can make configuration ergonomic without hiding the Config fields. Taking self by value is a common builder-style pattern.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "impl_method",
            "implFor": "Config",
            "methodName": "with_service_url",
            "requiredSignatureIncludes": [
              "self",
              "impl Into<String>",
              "Self"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std"
      }
    ]
  }
}
