{
  "schemaVersion": 2,
  "id": "config-err-display-002",
  "arcId": "config-loader-errors",
  "arcTitle": "Hierarchical configuration loader errors",
  "order": 62,
  "day": 2,
  "arcLength": 6,
  "title": "Separate error kinds from error messages",
  "conceptId": "config-err-display",
  "difficulty": "easy",
  "estimatedMinutes": 7,
  "scenario": "Typed errors are for programs; Display provides stable human-readable messages at the boundary.",
  "instructions": "Keep the derived thiserror messages. Add ConfigLoadError::kind(&self) -> &'static str with stable snake_case values for each variant.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "use thiserror::Error;\n\n#[derive(Debug, Error)]\npub enum ConfigLoadError {\n    #[error(\"missing APP_PORT\")]\n    MissingEnvironment,\n    #[error(\"invalid APP_PORT\")]\n    InvalidPort,\n    #[error(\"failed to read config file\")]\n    FileRead,\n}\n\n// TODO: add a stable programmatic kind method.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use rust_daily_lesson::ConfigLoadError;\n\n#[test]\nfn exposes_stable_error_kinds() {\n    assert_eq!(ConfigLoadError::MissingEnvironment.kind(), \"missing_environment\");\n    assert_eq!(ConfigLoadError::InvalidPort.kind(), \"invalid_port\");\n    assert_eq!(ConfigLoadError::FileRead.kind(), \"file_read\");\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Display text is for people; a kind value is for logs and metrics."
    },
    {
      "level": 2,
      "body": "Match on self and return a static string."
    },
    {
      "level": 3,
      "body": "Do not parse the Display message to classify errors.",
      "solutionCode": "use thiserror::Error;\n\n#[derive(Debug, Error)]\npub enum ConfigLoadError {\n    #[error(\"missing APP_PORT\")]\n    MissingEnvironment,\n    #[error(\"invalid APP_PORT\")]\n    InvalidPort,\n    #[error(\"failed to read config file\")]\n    FileRead,\n}\n\nimpl ConfigLoadError {\n    pub fn kind(&self) -> &'static str {\n        match self {\n            Self::MissingEnvironment => \"missing_environment\",\n            Self::InvalidPort => \"invalid_port\",\n            Self::FileRead => \"file_read\",\n        }\n    }\n}"
    }
  ],
  "completionExplanation": "The error now has both human-readable text and a stable machine-readable classification.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "impl_method",
            "implFor": "ConfigLoadError",
            "methodName": "kind",
            "requiredSignatureIncludes": [
              "fn kind(&self) -> &str"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "advanced"
      }
    ]
  }
}
