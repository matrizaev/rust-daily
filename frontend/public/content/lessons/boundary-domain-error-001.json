{
  "schemaVersion": 2,
  "id": "boundary-domain-error-001",
  "arcId": "boundary-error-mapping",
  "arcTitle": "Translating errors across boundaries",
  "order": 67,
  "day": 1,
  "arcLength": 6,
  "title": "Define domain order errors",
  "conceptId": "boundary-domain-error",
  "difficulty": "easy",
  "estimatedMinutes": 6,
  "scenario": "Domain errors should name business-rule failures without mentioning databases, HTTP, or UI details.",
  "instructions": "Define CreateOrderError with EmptyOrder and InvalidQuantity variants.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "// TODO: define CreateOrderError for domain rule failures.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use rust_daily_lesson::CreateOrderError;\n\n#[test]\nfn domain_errors_are_programmatic() {\n    assert_eq!(CreateOrderError::EmptyOrder, CreateOrderError::EmptyOrder);\n    assert_ne!(CreateOrderError::EmptyOrder, CreateOrderError::InvalidQuantity);\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Keep the Translating errors across boundaries code active and define CreateOrderError with EmptyOrder and InvalidQuantity variants."
    },
    {
      "level": 2,
      "body": "Use the Rust API named in this lesson instead of adding a side helper; the previous arc code should stay in the main solution."
    },
    {
      "level": 3,
      "body": "One complete solution for Define domain order errors is shown below.",
      "solutionCode": "#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum CreateOrderError {\n    EmptyOrder,\n    InvalidQuantity,\n}"
    }
  ],
  "completionExplanation": "Define domain order errors now extends the active Translating errors across boundaries code with boundary domain error. The previous lesson's behavior remains in the main solution rather than an archived module.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "enum_unit_variants",
            "enumName": "CreateOrderError",
            "requiredVariants": [
              "EmptyOrder",
              "InvalidQuantity"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std"
      }
    ]
  }
}
