{
  "schemaVersion": 2,
  "id": "inventory-intoiterator-005",
  "arcId": "inventory-summary",
  "arcTitle": "Summarize inventory",
  "order": 23,
  "day": 5,
  "arcLength": 6,
  "title": "Make Inventory iterable",
  "conceptId": "intoiterator-wrapper",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "A wrapper type can expose natural iteration while still owning its internal Vec.",
  "instructions": "The starter already defines Inventory with a private Vec<Item>. Implement IntoIterator for Inventory so consuming the wrapper yields Item values.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "#[derive(Debug, Clone, PartialEq, Eq)]\npub struct Item {\n    pub sku: String,\n    pub name: String,\n    pub quantity: u32,\n    pub reserved: u32,\n}\n\npub fn available_names(items: &[Item]) -> Vec<&str> {\n    items\n        .iter()\n        .filter_map(|item| {\n            if item.quantity > item.reserved {\n                Some(item.name.as_str())\n            } else {\n                None\n            }\n        })\n        .collect()\n}\n\npub fn total_quantity(items: &[Item]) -> u32 {\n    items.iter().fold(0, |total, item| total + item.quantity)\n}\n\npub fn reorder_notes(notes: &[String]) -> Vec<String> {\n    let mut relevant_notes = Vec::new();\n\n    for note in notes {\n        let normalized = note.trim().to_lowercase();\n        if normalized.is_empty() {\n            continue;\n        }\n\n        let is_inventory_note = normalized.contains(\"urgent\") || normalized.contains(\"stock\");\n        if is_inventory_note {\n            relevant_notes.push(normalized);\n        }\n    }\n\n    relevant_notes\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct Inventory {\n    items: Vec<Item>,\n}\n\nimpl Inventory {\n    pub fn new(items: Vec<Item>) -> Self {\n        Self { items }\n    }\n}\n\n// Continue from the previous lesson.\n// TODO: implement IntoIterator for Inventory.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use rust_daily_lesson::{Inventory, Item};\n\n#[test]\nfn inventory_consumes_into_items() {\n    let inventory = Inventory::new(vec![\n        Item {\n            sku: \"keyboard\".to_owned(),\n            name: \"Keyboard\".to_owned(),\n            quantity: 10,\n            reserved: 0,\n        },\n        Item {\n            sku: \"mouse\".to_owned(),\n            name: \"Mouse\".to_owned(),\n            quantity: 4,\n            reserved: 0,\n        },\n    ]);\n    let names: Vec<_> = inventory.into_iter().map(|item| item.name).collect();\n\n    assert_eq!(names, vec![\"Keyboard\".to_owned(), \"Mouse\".to_owned()]);\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Keep Inventory's items field private; the iterator can consume it from inside the impl."
    },
    {
      "level": 2,
      "body": "Vec<Item> already has an into_iter method that consumes the vector."
    },
    {
      "level": 3,
      "body": "The associated IntoIter type can reuse std::vec::IntoIter<Item>.",
      "solutionCode": "#[derive(Debug, Clone, PartialEq, Eq)]\npub struct Item {\n    pub sku: String,\n    pub name: String,\n    pub quantity: u32,\n    pub reserved: u32,\n}\n\npub fn available_names(items: &[Item]) -> Vec<&str> {\n    items\n        .iter()\n        .filter_map(|item| {\n            if item.quantity > item.reserved {\n                Some(item.name.as_str())\n            } else {\n                None\n            }\n        })\n        .collect()\n}\n\npub fn total_quantity(items: &[Item]) -> u32 {\n    items.iter().fold(0, |total, item| total + item.quantity)\n}\n\npub fn reorder_notes(notes: &[String]) -> Vec<String> {\n    let mut relevant_notes = Vec::new();\n\n    for note in notes {\n        let normalized = note.trim().to_lowercase();\n        if normalized.is_empty() {\n            continue;\n        }\n\n        let is_inventory_note = normalized.contains(\"urgent\") || normalized.contains(\"stock\");\n        if is_inventory_note {\n            relevant_notes.push(normalized);\n        }\n    }\n\n    relevant_notes\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct Inventory {\n    items: Vec<Item>,\n}\n\nimpl Inventory {\n    pub fn new(items: Vec<Item>) -> Self {\n        Self { items }\n    }\n}\n\nimpl IntoIterator for Inventory {\n    type Item = Item;\n    type IntoIter = std::vec::IntoIter<Item>;\n\n    fn into_iter(self) -> Self::IntoIter {\n        self.items.into_iter()\n    }\n}"
    }
  ],
  "completionExplanation": "IntoIterator lets Inventory participate in for loops and iterator adapters without exposing its internal field as public API.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "impl_trait_for_type",
            "traitName": "IntoIterator",
            "typeName": "Inventory"
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "type Item = Item",
              "std::vec::IntoIter<Item>"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std"
      }
    ]
  }
}
