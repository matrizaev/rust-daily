{
  "schemaVersion": 2,
  "id": "percentage-doc-example-005",
  "arcId": "table-driven-domain-tests",
  "arcTitle": "Table-driven domain tests",
  "order": 89,
  "day": 5,
  "arcLength": 6,
  "title": "Document Percentage with an executable example",
  "conceptId": "percentage-doc-example",
  "difficulty": "easy",
  "estimatedMinutes": 7,
  "scenario": "A doc example should show normal usage and handle fallible APIs without unwrap or expect.",
  "instructions": "Add a doc comment example for Percentage that asserts on the Result returned by TryFrom.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct Percentage(u8);\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum PercentageError {\n    OutOfRange,\n}\n\nimpl Percentage {\n    pub fn value(&self) -> u8 {\n        self.0\n    }\n}\n\nimpl TryFrom<u16> for Percentage {\n    type Error = PercentageError;\n\n    fn try_from(value: u16) -> Result<Self, Self::Error> {\n        if value > 100 {\n            return Err(PercentageError::OutOfRange);\n        }\n\n        Ok(Self(value as u8))\n    }\n}\n\n\n// TODO: add a doc comment example above Percentage in a real implementation.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use rust_daily_lesson::Percentage;\n\n#[test]\nfn documented_percentage_behavior_works() {\n    assert_eq!(Percentage::try_from(75).map(|value| value.value()), Ok(75));\n    assert!(Percentage::try_from(101).is_err());\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Focus on the one public API shape requested by the lesson."
    },
    {
      "level": 2,
      "body": "Prefer standard traits, typed errors, borrowed views, and explicit boundary conversions."
    },
    {
      "level": 3,
      "body": "A canonical idiomatic solution is shown below.",
      "solutionCode": "/// A bounded percentage value from 0 through 100.\n///\n/// ```\n/// use rust_daily_lesson::Percentage;\n///\n/// assert_eq!(Percentage::try_from(75).map(|value| value.value()), Ok(75));\n/// assert!(Percentage::try_from(101).is_err());\n/// ```\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct Percentage(u8);\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum PercentageError {\n    OutOfRange,\n}\n\nimpl Percentage {\n    pub fn value(&self) -> u8 {\n        self.0\n    }\n}\n\nimpl TryFrom<u16> for Percentage {\n    type Error = PercentageError;\n\n    fn try_from(value: u16) -> Result<Self, Self::Error> {\n        if value > 100 {\n            return Err(PercentageError::OutOfRange);\n        }\n\n        Ok(Self(value as u8))\n    }\n}"
    }
  ],
  "completionExplanation": "This lesson keeps percentage-doc-example explicit in the type system and checks public behavior without relying on panics or unchecked shortcuts.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "source_includes",
            "requiredSnippets": [
              "///",
              "Percentage::try_from(75)",
              "is_err()"
            ],
            "forbiddenSnippets": [
              "unwrap",
              "expect",
              "panic!"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std"
      }
    ]
  }
}
