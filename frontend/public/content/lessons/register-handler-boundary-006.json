{
  "schemaVersion": 2,
  "id": "register-handler-boundary-006",
  "arcId": "register-user-use-case",
  "arcTitle": "Register user use case boundaries",
  "order": 78,
  "day": 6,
  "arcLength": 6,
  "title": "Map a handler boundary without framework leakage",
  "conceptId": "register-handler-boundary",
  "difficulty": "medium",
  "estimatedMinutes": 10,
  "scenario": "A handler adapter should translate request DTOs, call the use case, and return response data without pushing HTTP concepts into domain code.",
  "instructions": "Implement handle_register_user by converting the request, calling register_user, and mapping outcomes to Response values.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "pub mod domain {\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct EmailAddress(pub String);\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct UserId(pub u64);\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct RegisterUserCommand { pub email: EmailAddress, pub display_name: String }\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct NewUser { pub email: EmailAddress, pub display_name: String }\n}\n\npub mod application {\n    use super::domain::{EmailAddress, NewUser, RegisterUserCommand, UserId};\n    #[derive(Debug, Clone, Copy, PartialEq, Eq)]\n    pub enum RepositoryError { Unavailable }\n    #[derive(Debug, Clone, Copy, PartialEq, Eq)]\n    pub enum RegisterUserError { DuplicateEmail, Repository(RepositoryError) }\n    pub trait UserRepository {\n        fn exists_by_email(&self, email: &EmailAddress) -> Result<bool, RepositoryError>;\n        fn save(&mut self, user: NewUser) -> Result<UserId, RepositoryError>;\n    }\n\n    pub fn register_user<R: UserRepository>(repo: &mut R, command: RegisterUserCommand) -> Result<UserId, RegisterUserError> {\n        if repo.exists_by_email(&command.email).map_err(RegisterUserError::Repository)? {\n            return Err(RegisterUserError::DuplicateEmail);\n        }\n\n        repo.save(NewUser { email: command.email, display_name: command.display_name })\n            .map_err(RegisterUserError::Repository)\n    }\n}\n\npub mod adapters {\n    use super::domain::{EmailAddress, RegisterUserCommand};\n\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct RegisterUserRequest { pub email: String, pub display_name: String }\n    #[derive(Debug, Clone, Copy, PartialEq, Eq)]\n    pub enum RequestError { EmptyEmail, EmptyDisplayName }\n\n    impl TryFrom<RegisterUserRequest> for RegisterUserCommand {\n        type Error = RequestError;\n\n        fn try_from(value: RegisterUserRequest) -> Result<Self, Self::Error> {\n            let email = value.email.trim();\n            let display_name = value.display_name.trim();\n            if email.is_empty() { return Err(RequestError::EmptyEmail); }\n            if display_name.is_empty() { return Err(RequestError::EmptyDisplayName); }\n\n            Ok(Self { email: EmailAddress(email.to_owned()), display_name: display_name.to_owned() })\n        }\n    }\n}\n\npub mod infrastructure {\n    use super::application::{RepositoryError, UserRepository};\n    use super::domain::{EmailAddress, NewUser, UserId};\n\n    pub struct InMemoryUserRepository { users: Vec<NewUser>, next_id: u64 }\n\n    impl InMemoryUserRepository { pub fn new() -> Self { Self { users: Vec::new(), next_id: 1 } } }\n\n    impl UserRepository for InMemoryUserRepository {\n        fn exists_by_email(&self, email: &EmailAddress) -> Result<bool, RepositoryError> {\n            Ok(self.users.iter().any(|user| user.email == *email))\n        }\n\n        fn save(&mut self, user: NewUser) -> Result<UserId, RepositoryError> {\n            let id = UserId(self.next_id);\n            self.next_id += 1;\n            self.users.push(user);\n            Ok(id)\n        }\n    }\n}\n\n// Continue from the previous lesson.\n// TODO: implement handle_register_user.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use rust_daily_lesson::{handle_register_user, RegisterUserCommand, RegisterUserError, RegisterUserRequest, RegisterUserUseCase, UserId};\n\nstruct FakeUseCase { result: Result<UserId, RegisterUserError> }\nimpl RegisterUserUseCase for FakeUseCase {\n    fn register_user(&mut self, _command: RegisterUserCommand) -> Result<UserId, RegisterUserError> { self.result }\n}\n\n#[test]\nfn handler_maps_usecase_outcomes() {\n    let request = RegisterUserRequest { email: \"ada@example.com\".to_owned(), display_name: \"Ada\".to_owned() };\n    let mut ok = FakeUseCase { result: Ok(UserId(1)) };\n    let mut duplicate = FakeUseCase { result: Err(RegisterUserError::DuplicateEmail) };\n\n    assert_eq!(handle_register_user(&mut ok, request.clone()).status, 201);\n    assert_eq!(handle_register_user(&mut duplicate, request).status, 409);\n}\n\n#[test]\nfn handler_rejects_invalid_request_before_usecase() {\n    let mut use_case = FakeUseCase { result: Ok(UserId(1)) };\n    let response = handle_register_user(&mut use_case, RegisterUserRequest { email: \" \".to_owned(), display_name: \"Ada\".to_owned() });\n\n    assert_eq!(response.status, 400);\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Keep the Register user use case boundaries code active and implement handle_register_user by converting the request, calling register_user, and mapping outcomes to Response values."
    },
    {
      "level": 2,
      "body": "Use the Rust API named in this lesson instead of adding a side helper; the previous arc code should stay in the main solution."
    },
    {
      "level": 3,
      "body": "One complete solution for Map a handler boundary without framework leakage is shown below.",
      "solutionCode": "pub mod domain {\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct EmailAddress(String);\n\n    impl EmailAddress {\n        pub fn new(value: impl Into<String>) -> Self {\n            Self(value.into())\n        }\n\n        pub fn as_str(&self) -> &str {\n            &self.0\n        }\n    }\n\n    #[derive(Debug, Clone, Copy, PartialEq, Eq)]\n    pub struct UserId(pub u64);\n\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct RegisterUserCommand {\n        email: EmailAddress,\n        display_name: String,\n    }\n\n    impl RegisterUserCommand {\n        pub fn new(email: EmailAddress, display_name: impl Into<String>) -> Self {\n            Self {\n                email,\n                display_name: display_name.into(),\n            }\n        }\n\n        pub fn email(&self) -> &EmailAddress {\n            &self.email\n        }\n\n        pub fn display_name(&self) -> &str {\n            &self.display_name\n        }\n    }\n\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct NewUser {\n        pub email: EmailAddress,\n        pub display_name: String,\n    }\n\n    impl From<RegisterUserCommand> for NewUser {\n        fn from(command: RegisterUserCommand) -> Self {\n            Self {\n                email: command.email,\n                display_name: command.display_name,\n            }\n        }\n    }\n}\n\npub mod application {\n    use super::domain::{NewUser, RegisterUserCommand, UserId};\n\n    #[derive(Debug, Clone, Copy, PartialEq, Eq)]\n    pub enum RegisterUserError {\n        InvalidRequest,\n        DuplicateEmail,\n        RepositoryUnavailable,\n    }\n\n    #[derive(Debug, Clone, Copy, PartialEq, Eq)]\n    pub enum RepositoryError {\n        Unavailable,\n    }\n\n    pub trait RegisterUserUseCase {\n        fn register_user(\n            &mut self,\n            command: RegisterUserCommand,\n        ) -> Result<UserId, RegisterUserError>;\n    }\n\n    pub trait UserRepository {\n        fn exists_by_email(\n            &self,\n            email: &super::domain::EmailAddress,\n        ) -> Result<bool, RepositoryError>;\n\n        fn save(&mut self, user: NewUser) -> Result<UserId, RepositoryError>;\n    }\n\n    pub fn register_user<R: UserRepository>(\n        repository: &mut R,\n        command: RegisterUserCommand,\n    ) -> Result<UserId, RegisterUserError> {\n        if repository\n            .exists_by_email(command.email())\n            .map_err(|_| RegisterUserError::RepositoryUnavailable)?\n        {\n            return Err(RegisterUserError::DuplicateEmail);\n        }\n\n        repository\n            .save(NewUser::from(command))\n            .map_err(|_| RegisterUserError::RepositoryUnavailable)\n    }\n}\n\npub mod adapters {\n    use super::application::{RegisterUserError, RegisterUserUseCase};\n    use super::domain::{EmailAddress, RegisterUserCommand};\n\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct RegisterUserRequest {\n        pub email: String,\n        pub display_name: String,\n    }\n\n    #[derive(Debug, Clone, Copy, PartialEq, Eq)]\n    pub struct Response {\n        pub status: u16,\n    }\n\n    impl TryFrom<RegisterUserRequest> for RegisterUserCommand {\n        type Error = RegisterUserError;\n\n        fn try_from(value: RegisterUserRequest) -> Result<Self, Self::Error> {\n            let email = value.email.trim();\n            let display_name = value.display_name.trim();\n\n            if email.is_empty() || display_name.is_empty() {\n                return Err(RegisterUserError::InvalidRequest);\n            }\n\n            Ok(RegisterUserCommand::new(\n                EmailAddress::new(email.to_owned()),\n                display_name.to_owned(),\n            ))\n        }\n    }\n\n    pub fn handle_register_user<U: RegisterUserUseCase>(\n        use_case: &mut U,\n        request: RegisterUserRequest,\n    ) -> Response {\n        let command = match RegisterUserCommand::try_from(request) {\n            Ok(command) => command,\n            Err(RegisterUserError::InvalidRequest) => return Response { status: 400 },\n            Err(RegisterUserError::DuplicateEmail | RegisterUserError::RepositoryUnavailable) => {\n                return Response { status: 500 };\n            }\n        };\n\n        match use_case.register_user(command) {\n            Ok(_) => Response { status: 201 },\n            Err(RegisterUserError::InvalidRequest) => Response { status: 400 },\n            Err(RegisterUserError::DuplicateEmail) => Response { status: 409 },\n            Err(RegisterUserError::RepositoryUnavailable) => Response { status: 503 },\n        }\n    }\n}\n\npub mod infrastructure {\n    use super::application::{RepositoryError, UserRepository};\n    use super::domain::{EmailAddress, NewUser, UserId};\n\n    #[derive(Debug, Default)]\n    pub struct InMemoryUserRepository {\n        users: Vec<NewUser>,\n        next_id: u64,\n    }\n\n    impl InMemoryUserRepository {\n        pub fn new() -> Self {\n            Self {\n                users: Vec::new(),\n                next_id: 1,\n            }\n        }\n    }\n\n    impl UserRepository for InMemoryUserRepository {\n        fn exists_by_email(&self, email: &EmailAddress) -> Result<bool, RepositoryError> {\n            Ok(self.users.iter().any(|user| &user.email == email))\n        }\n\n        fn save(&mut self, user: NewUser) -> Result<UserId, RepositoryError> {\n            let id = UserId(self.next_id);\n            self.next_id += 1;\n            self.users.push(user);\n            Ok(id)\n        }\n    }\n}\n\npub use adapters::{handle_register_user, RegisterUserRequest, Response};\npub use application::{RegisterUserError, RegisterUserUseCase};\npub use domain::{RegisterUserCommand, UserId};"
    }
  ],
  "completionExplanation": "Map a handler boundary without framework leakage now extends the active Register user use case boundaries code with register handler boundary. The previous lesson's behavior remains in the main solution rather than an archived module.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "function_signature",
            "functionName": "handle_register_user",
            "requiredSignatureIncludes": [
              "RegisterUserUseCase",
              "Response"
            ]
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "RegisterUserCommand::try_from",
              "use_case.register_user",
              "201",
              "409",
              "503"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std"
      }
    ]
  }
}
