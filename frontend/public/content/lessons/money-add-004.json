{
  "schemaVersion": 2,
  "id": "money-add-004",
  "arcId": "money-value-object",
  "arcTitle": "Money and currency domain representation",
  "order": 40,
  "day": 4,
  "arcLength": 6,
  "title": "Add Money with a typed error",
  "conceptId": "money-add-impl",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "Callers need to add two money values, but currency mismatches and integer overflow are expected domain failures. Library code should return typed errors instead of panicking.",
  "instructions": "Implement Money::checked_add(self, rhs: Self) -> Result<Self, MoneyAddError>. Return CurrencyMismatch for different currencies and AmountOverflow for checked integer overflow.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum Currency {\n    Usd,\n    Eur,\n    Gbp,\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct Money {\n    amount: u64,\n    currency: Currency,\n}\n\nimpl Money {\n    pub fn new(amount: u64, currency: Currency) -> Self {\n        Self { amount, currency }\n    }\n\n    pub fn amount(&self) -> u64 {\n        self.amount\n    }\n\n    pub fn currency(&self) -> Currency {\n        self.currency\n    }\n}\n\n// Continue from the previous lesson.\n// TODO: Implement checked_add without panicking.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use rust_daily_lesson::{Currency, Money, MoneyAddError};\n\n#[test]\nfn adds_same_currency() {\n    let first = Money::new(100, Currency::Usd);\n    let second = Money::new(50, Currency::Usd);\n\n    assert_eq!(first.checked_add(second), Ok(Money::new(150, Currency::Usd)));\n}\n\n#[test]\nfn rejects_different_currencies() {\n    let first = Money::new(100, Currency::Usd);\n    let second = Money::new(50, Currency::Eur);\n\n    assert_eq!(\n        first.checked_add(second),\n        Err(MoneyAddError::CurrencyMismatch {\n            left: Currency::Usd,\n            right: Currency::Eur,\n        })\n    );\n}\n\n#[test]\nfn reports_amount_overflow() {\n    let first = Money::new(u64::MAX, Currency::Gbp);\n    let second = Money::new(1, Currency::Gbp);\n\n    assert_eq!(first.checked_add(second), Err(MoneyAddError::AmountOverflow));\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Keep this as a method returning Result instead of implementing an operator that can panic."
    },
    {
      "level": 2,
      "body": "Check currency equality first, then use u64::checked_add to combine the minor-unit amounts safely."
    },
    {
      "level": 3,
      "body": "A canonical solution returns programmatic errors for both domain and arithmetic failures.",
      "solutionCode": "#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum Currency {\n    Usd,\n    Eur,\n    Gbp,\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct Money {\n    amount: u64,\n    currency: Currency,\n}\n\nimpl Money {\n    pub fn new(amount: u64, currency: Currency) -> Self {\n        Self { amount, currency }\n    }\n\n    pub fn amount(&self) -> u64 {\n        self.amount\n    }\n\n    pub fn currency(&self) -> Currency {\n        self.currency\n    }\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum MoneyAddError {\n    CurrencyMismatch { left: Currency, right: Currency },\n    AmountOverflow,\n}\n\nimpl Money {\n    pub fn checked_add(self, rhs: Self) -> Result<Self, MoneyAddError> {\n        if self.currency != rhs.currency {\n            return Err(MoneyAddError::CurrencyMismatch {\n                left: self.currency,\n                right: rhs.currency,\n            });\n        }\n\n        let amount = self\n            .amount\n            .checked_add(rhs.amount)\n            .ok_or(MoneyAddError::AmountOverflow)?;\n\n        Ok(Self {\n            amount,\n            currency: self.currency,\n        })\n    }\n}"
    }
  ],
  "completionExplanation": "Fallible domain operations should make failure visible in the type signature. A Result keeps currency mismatches and overflow recoverable and testable, while avoiding panics in library code.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "impl_method",
            "implFor": "Money",
            "methodName": "checked_add",
            "requiredSignatureIncludes": [
              "pub fn checked_add",
              "rhs: Self",
              "Result<Self, MoneyAddError>"
            ]
          },
          {
            "type": "enum_unit_variants",
            "enumName": "MoneyAddError",
            "requiredVariants": [
              "AmountOverflow"
            ]
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "CurrencyMismatch",
              "checked_add"
            ],
            "forbiddenSnippets": [
              "panic!",
              ".unwrap()",
              ".expect(",
              "impl Add for Money"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std"
      }
    ]
  }
}
