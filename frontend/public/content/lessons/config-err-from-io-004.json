{
  "schemaVersion": 2,
  "id": "config-err-from-io-004",
  "arcId": "config-loader-errors",
  "arcTitle": "Hierarchical configuration loader errors",
  "order": 64,
  "day": 4,
  "arcLength": 6,
  "title": "Convert io::Error with From",
  "conceptId": "config-err-from-io",
  "difficulty": "medium",
  "estimatedMinutes": 7,
  "scenario": "A From implementation centralizes how low-level I/O errors become configuration errors and enables ? at call sites.",
  "instructions": "Implement From<std::io::Error> for ConfigLoadError by returning ConfigLoadError::FileRead(error).",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "use std::io;\n\n#[derive(Debug)]\npub enum ConfigLoadError {\n    FileRead(io::Error),\n}\n\n// TODO: implement From<io::Error> for ConfigLoadError.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use std::io;\n\nuse rust_daily_lesson::ConfigLoadError;\n\n#[test]\nfn converts_io_error_into_config_error() {\n    let converted = ConfigLoadError::from(io::Error::new(io::ErrorKind::NotFound, \"missing\"));\n\n    assert!(matches!(converted, ConfigLoadError::FileRead(_)));\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Focus on the one public API shape requested by the lesson."
    },
    {
      "level": 2,
      "body": "Prefer standard traits, typed errors, borrowed views, and explicit boundary conversions."
    },
    {
      "level": 3,
      "body": "A canonical idiomatic solution is shown below.",
      "solutionCode": "use std::io;\n\n#[derive(Debug)]\npub enum ConfigLoadError {\n    FileRead(io::Error),\n}\n\nimpl From<io::Error> for ConfigLoadError {\n    fn from(error: io::Error) -> Self {\n        ConfigLoadError::FileRead(error)\n    }\n}"
    }
  ],
  "completionExplanation": "This lesson keeps config-err-from-io explicit in the type system and checks public behavior without relying on panics or unchecked shortcuts.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "impl_trait_for_type",
            "traitName": "From<io::Error>",
            "typeName": "ConfigLoadError"
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "fn from",
              "ConfigLoadError::FileRead"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std"
      }
    ]
  }
}
