{
  "schemaVersion": 2,
  "id": "config-load-port-006",
  "arcId": "config-loader-errors",
  "arcTitle": "Hierarchical configuration loader errors",
  "order": 66,
  "day": 6,
  "arcLength": 6,
  "title": "Propagate loader errors with ?",
  "conceptId": "config-load-error-propagation",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "Once conversions exist, loader functions can stay small and let ? preserve the typed error boundary.",
  "instructions": "Implement load_port by reading APP_PORT from a lookup function and parsing it with parse_port.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "use std::error::Error;\nuse std::fmt;\nuse std::io;\n\nuse std::num::ParseIntError;\n\n#[derive(Debug)]\npub enum ConfigLoadError {\n    MissingEnvironment,\n    InvalidPort(ParseIntError),\n    FileRead(io::Error),\n}\n\nimpl From<ParseIntError> for ConfigLoadError {\n    fn from(error: ParseIntError) -> Self {\n        ConfigLoadError::InvalidPort(error)\n    }\n}\n\npub fn parse_port(value: &str) -> Result<u16, ConfigLoadError> {\n    let port = value.parse::<u16>()?;\n\n    Ok(port)\n}\n\nimpl fmt::Display for ConfigLoadError {\n    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {\n        match self {\n            ConfigLoadError::MissingEnvironment => write!(f, \"missing environment\"),\n            ConfigLoadError::InvalidPort(_) => write!(f, \"invalid port\"),\n            ConfigLoadError::FileRead(_) => write!(f, \"could not read config file\"),\n        }\n    }\n}\n\nimpl Error for ConfigLoadError {\n    fn source(&self) -> Option<&(dyn Error + 'static)> {\n        match self {\n            ConfigLoadError::FileRead(error) => Some(error),\n            ConfigLoadError::InvalidPort(error) => Some(error),\n            ConfigLoadError::MissingEnvironment => None,\n        }\n    }\n}\n\nimpl From<io::Error> for ConfigLoadError {\n    fn from(error: io::Error) -> Self {\n        ConfigLoadError::FileRead(error)\n    }\n}\n\n// Continue from the previous lesson.\n// TODO: implement load_port using the provided lookup function.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use rust_daily_lesson::{load_port, ConfigLoadError};\n\n#[test]\nfn loads_port_from_lookup() {\n    assert!(matches!(load_port(|_| Some(\"8080\".to_owned())), Ok(8080)));\n}\n\n#[test]\nfn reports_missing_and_invalid_port() {\n    assert!(matches!(\n        load_port(|_| None),\n        Err(ConfigLoadError::MissingEnvironment)\n    ));\n    assert!(matches!(\n        load_port(|_| Some(\"abc\".to_owned())),\n        Err(ConfigLoadError::InvalidPort(_)),\n    ));\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Keep the Hierarchical configuration loader errors code active and implement load_port by reading APP_PORT from a lookup function and parsing it with parse_port."
    },
    {
      "level": 2,
      "body": "Use the Rust API named in this lesson instead of adding a side helper; the previous arc code should stay in the main solution."
    },
    {
      "level": 3,
      "body": "One complete solution for Propagate loader errors with ? is shown below.",
      "solutionCode": "use std::error::Error;\nuse std::fmt;\nuse std::io;\nuse std::num::ParseIntError;\n\n#[derive(Debug)]\npub enum ConfigLoadError {\n    MissingEnvironment,\n    InvalidPort(ParseIntError),\n    FileRead(io::Error),\n}\n\nimpl fmt::Display for ConfigLoadError {\n    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {\n        match self {\n            ConfigLoadError::MissingEnvironment => write!(f, \"missing environment\"),\n            ConfigLoadError::InvalidPort(_) => write!(f, \"invalid port\"),\n            ConfigLoadError::FileRead(_) => write!(f, \"could not read config file\"),\n        }\n    }\n}\n\nimpl Error for ConfigLoadError {\n    fn source(&self) -> Option<&(dyn Error + 'static)> {\n        match self {\n            ConfigLoadError::InvalidPort(error) => Some(error),\n            ConfigLoadError::FileRead(error) => Some(error),\n            ConfigLoadError::MissingEnvironment => None,\n        }\n    }\n}\n\nimpl From<io::Error> for ConfigLoadError {\n    fn from(error: io::Error) -> Self {\n        ConfigLoadError::FileRead(error)\n    }\n}\n\nimpl From<ParseIntError> for ConfigLoadError {\n    fn from(error: ParseIntError) -> Self {\n        ConfigLoadError::InvalidPort(error)\n    }\n}\n\npub fn parse_port(value: &str) -> Result<u16, ConfigLoadError> {\n    Ok(value.parse::<u16>()?)\n}\n\npub fn load_port(lookup: impl Fn(&str) -> Option<String>) -> Result<u16, ConfigLoadError> {\n    let raw = lookup(\"APP_PORT\").ok_or(ConfigLoadError::MissingEnvironment)?;\n    let port = parse_port(raw.as_str())?;\n\n    Ok(port)\n}"
    }
  ],
  "completionExplanation": "Propagate loader errors with ? now extends the active Hierarchical configuration loader errors code with config load error propagation. The previous lesson's behavior remains in the main solution rather than an archived module.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "function_signature",
            "functionName": "load_port",
            "requiredSignatureIncludes": [
              "Result<u16, ConfigLoadError>"
            ]
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "ok_or(ConfigLoadError::MissingEnvironment)?",
              "parse_port(raw.as_str())?"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std"
      }
    ]
  }
}
