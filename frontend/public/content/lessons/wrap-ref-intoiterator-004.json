{
  "schemaVersion": 2,
  "id": "wrap-ref-intoiterator-004",
  "arcId": "collection-wrappers",
  "arcTitle": "Exposing iteration on collection wrappers",
  "order": 58,
  "day": 4,
  "arcLength": 6,
  "title": "Iterate over borrowed collection wrappers",
  "conceptId": "wrap-ref-into-iterator",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "For loops over &OrderLines should borrow each line instead of consuming the wrapper.",
  "instructions": "The starter already supports owned IntoIterator. Implement IntoIterator for &OrderLines, yielding &OrderLine values.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "content": "#[derive(Debug, Clone, PartialEq, Eq)]\npub struct OrderLine {\n    pub sku: String,\n    pub quantity: u32,\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct OrderLines {\n    lines: Vec<OrderLine>,\n}\n\nimpl OrderLines {\n    pub fn new(lines: Vec<OrderLine>) -> Self {\n        Self { lines }\n    }\n\n    pub fn len(&self) -> usize {\n        self.lines.len()\n    }\n\n    pub fn is_empty(&self) -> bool {\n        self.lines.is_empty()\n    }\n\n    pub fn iter(&self) -> std::slice::Iter<'_, OrderLine> {\n        self.lines.iter()\n    }\n}\n\nimpl IntoIterator for OrderLines {\n    type Item = OrderLine;\n    type IntoIter = std::vec::IntoIter<OrderLine>;\n\n    fn into_iter(self) -> Self::IntoIter {\n        self.lines.into_iter()\n    }\n}\n\n// Continue from the previous lesson.\n// TODO: implement IntoIterator for &OrderLines.\n"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "content": "use rust_daily_lesson::{OrderLine, OrderLines};\n\n#[test]\nfn borrowed_iteration_keeps_wrapper_available() {\n    let lines = OrderLines::new(vec![OrderLine {\n        sku: \"A\".to_owned(),\n        quantity: 2,\n    }]);\n    let total: u32 = (&lines).into_iter().map(|line| line.quantity).sum();\n    let skus: Vec<_> = (&lines).into_iter().map(|line| line.sku.as_str()).collect();\n\n    assert_eq!(total, 2);\n    assert_eq!(skus, vec![\"A\"]);\n}\n"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Implement the trait for &'a OrderLines with Item = &'a OrderLine."
    },
    {
      "level": 2,
      "body": "Return self.iter() from into_iter so the for-loop behavior reuses the borrowed iterator method."
    },
    {
      "level": 3,
      "body": "One complete solution for Iterate over borrowed collection wrappers is shown below.",
      "solutionCode": "#[derive(Debug, Clone, PartialEq, Eq)]\npub struct OrderLine {\n    pub sku: String,\n    pub quantity: u32,\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct OrderLines {\n    lines: Vec<OrderLine>,\n}\n\nimpl OrderLines {\n    pub fn new(lines: Vec<OrderLine>) -> Self {\n        Self { lines }\n    }\n\n    pub fn len(&self) -> usize {\n        self.lines.len()\n    }\n\n    pub fn is_empty(&self) -> bool {\n        self.lines.is_empty()\n    }\n\n    pub fn iter(&self) -> std::slice::Iter<'_, OrderLine> {\n        self.lines.iter()\n    }\n}\n\nimpl IntoIterator for OrderLines {\n    type Item = OrderLine;\n    type IntoIter = std::vec::IntoIter<OrderLine>;\n\n    fn into_iter(self) -> Self::IntoIter {\n        self.lines.into_iter()\n    }\n}\n\nimpl<'a> IntoIterator for &'a OrderLines {\n    type Item = &'a OrderLine;\n    type IntoIter = std::slice::Iter<'a, OrderLine>;\n\n    fn into_iter(self) -> Self::IntoIter {\n        self.iter()\n    }\n}"
    }
  ],
  "completionExplanation": "Borrowed IntoIterator enables `for line in &order_lines` while preserving owned iteration and the explicit iter method.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "source_includes",
            "requiredSnippets": [
              "impl<'a> IntoIterator for &'a OrderLines",
              "type Item = &'a OrderLine",
              "std::slice::Iter"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std"
      }
    ]
  }
}
