{
  "schemaVersion": 2,
  "id": "log-boundary-event-006",
  "arcId": "structured-request-logging",
  "arcTitle": "Structured request logging",
  "order": 84,
  "day": 6,
  "arcLength": 6,
  "title": "Create logging events at the application boundary",
  "conceptId": "log-boundary-event",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "Domain code should stay free of logging setup. Application boundaries can create events with meaningful fields.",
  "instructions": "Implement register_attempt_event that records request_id, optional user_id, and success.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Focus on the one public API shape requested by the lesson."
    },
    {
      "level": 2,
      "body": "Prefer standard traits, typed errors, borrowed views, and explicit boundary conversions."
    },
    {
      "level": 3,
      "body": "A canonical idiomatic solution is shown below.",
      "solutionCode": "#[derive(Debug, Clone, PartialEq, Eq)]\npub struct RegisterAttemptEvent {\n    pub event_name: String,\n    pub request_id: String,\n    pub user_id: Option<String>,\n    pub success: bool,\n}\n\npub fn register_attempt_event(\n    request_id: impl Into<String>,\n    user_id: Option<String>,\n    success: bool,\n) -> RegisterAttemptEvent {\n    RegisterAttemptEvent {\n        event_name: \"register_user.attempt\".to_owned(),\n        request_id: request_id.into(),\n        user_id,\n        success,\n    }\n}"
    }
  ],
  "completionExplanation": "This lesson keeps log-boundary-event explicit in the type system and checks public behavior without relying on panics or unchecked shortcuts.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "function_signature",
            "functionName": "register_attempt_event",
            "requiredSignatureIncludes": [
              "Option<String>",
              "RegisterAttemptEvent"
            ]
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "register_user.attempt"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
