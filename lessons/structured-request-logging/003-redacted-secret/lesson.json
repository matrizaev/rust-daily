{
  "schemaVersion": 2,
  "id": "log-redacted-secret-003",
  "arcId": "structured-request-logging",
  "arcTitle": "Structured request logging",
  "order": 81,
  "day": 3,
  "arcLength": 6,
  "title": "Redact secrets in formatted output",
  "conceptId": "log-redacted-secret",
  "difficulty": "medium",
  "estimatedMinutes": 7,
  "scenario": "Logging helpers should make accidental secret exposure difficult. A wrapper can intentionally hide sensitive values when formatted.",
  "instructions": "Define Secret and implement Display so it always writes [redacted].",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Keep the Structured request logging code active and define Secret and implement Display so it always writes [redacted]."
    },
    {
      "level": 2,
      "body": "Use the Rust API named in this lesson instead of adding a side helper; the previous arc code should stay in the main solution."
    },
    {
      "level": 3,
      "body": "One complete solution for Redact secrets in formatted output is shown below.",
      "solutionCode": "use std::fmt;\n\npub struct Secret(String);\n\nimpl Secret {\n    pub fn new(value: impl Into<String>) -> Self { Self(value.into()) }\n    pub fn expose(&self) -> &str { &self.0 }\n}\n\nimpl fmt::Display for Secret {\n    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {\n        write!(f, \"[redacted]\")\n    }\n}"
    }
  ],
  "completionExplanation": "Redact secrets in formatted output now extends the active Structured request logging code with log redacted secret. The previous lesson's behavior remains in the main solution rather than an archived module.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "impl_trait_for_type",
            "traitName": "fmt::Display",
            "typeName": "Secret"
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "[redacted]"
            ],
            "forbiddenSnippets": [
              "expose()"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
