{
  "schemaVersion": 2,
  "id": "percentage-named-cases-006",
  "arcId": "table-driven-domain-tests",
  "arcTitle": "Table-driven domain tests",
  "order": 90,
  "day": 6,
  "arcLength": 6,
  "title": "Combine named cases with property tests",
  "conceptId": "percentage-named-test-cases",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "Named cases make failing table-driven tests easier to diagnose and easier for humans to scan.",
  "instructions": "Replace repeated table boilerplate with a small macro_rules! helper that keeps case names visible. Add proptest properties covering every value in 0..=100 and every value above 100.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Keep the macro local to tests and focused on readability."
    },
    {
      "level": 2,
      "body": "Example cases document business-relevant points."
    },
    {
      "level": 3,
      "body": "Property tests complement examples by covering whole input ranges.",
      "solutionCode": "/// A bounded percentage from 0 through 100.\n///\n/// ```\n/// use rust_daily_lesson::Percentage;\n///\n/// let percentage = Percentage::try_from(75)?;\n/// assert_eq!(percentage.value(), 75);\n/// # Ok::<(), rust_daily_lesson::PercentageError>(())\n/// ```\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct Percentage(u8);\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum PercentageError {\n    OutOfRange,\n}\n\nimpl Percentage {\n    pub fn value(self) -> u8 {\n        self.0\n    }\n}\n\nimpl TryFrom<u16> for Percentage {\n    type Error = PercentageError;\n\n    fn try_from(value: u16) -> Result<Self, Self::Error> {\n        if value > 100 {\n            return Err(PercentageError::OutOfRange);\n        }\n\n        Ok(Self(value as u8))\n    }\n}\n\nimpl std::fmt::Display for Percentage {\n    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {\n        write!(formatter, \"{}%\", self.0)\n    }\n}\n\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    use proptest::prelude::*;\n\n    macro_rules! percentage_cases {\n        ($test_name:ident, { $($name:ident: $input:expr => $expected:expr),+ $(,)? }) => {\n            #[test]\n            fn $test_name() {\n                let cases = [$( (stringify!($name), $input, $expected), )+];\n\n                for (name, input, expected) in cases {\n                    let actual = Percentage::try_from(input).map(Percentage::value);\n                    assert_eq!(actual, expected, \"case failed: {name}\");\n                }\n            }\n        };\n    }\n\n    percentage_cases!(named_examples, {\n        zero: 0 => Ok(0),\n        middle: 50 => Ok(50),\n        maximum: 100 => Ok(100),\n        too_large: 101 => Err(PercentageError::OutOfRange),\n    });\n\n    proptest! {\n        #[test]\n        fn accepts_every_value_in_range(input in 0u16..=100) {\n            let percentage = Percentage::try_from(input).expect(\"generated value is in range\");\n            prop_assert_eq!(percentage.value(), input as u8);\n        }\n\n        #[test]\n        fn rejects_every_value_above_range(input in 101u16..=u16::MAX) {\n            prop_assert_eq!(\n                Percentage::try_from(input),\n                Err(PercentageError::OutOfRange)\n            );\n        }\n    }\n}"
    }
  ],
  "completionExplanation": "The final test suite combines readable named examples, executable documentation, and generated invariant coverage without hiding domain behavior behind macro complexity.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "source_includes",
            "requiredSnippets": [
              "macro_rules! percentage_cases",
              "proptest!",
              "0u16..=100",
              "101u16..=u16::MAX"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "advanced",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      },
      {
        "mode": "backend-compile-fail",
        "timeoutMs": 10000,
        "dependencySet": "advanced",
        "cases": [
          {
            "name": "percentage-direct-construction",
            "expectedDiagnostics": [
              "private"
            ],
            "sourcePath": "compile_fail/percentage_direct_construction.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
