{
  "schemaVersion": 2,
  "id": "percentage-invalid-table-tests-003",
  "arcId": "table-driven-domain-tests",
  "arcTitle": "Table-driven domain tests",
  "order": 87,
  "day": 3,
  "arcLength": 6,
  "title": "Write table-driven invalid examples",
  "conceptId": "percentage-invalid-table-tests",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "Invalid cases deserve precise tests too. Table-driven tests make it clear which inputs should fail.",
  "instructions": "Keep the valid table test. Add a second test that loops over several values above 100 and checks PercentageError::OutOfRange.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Invalid cases can share one expected error."
    },
    {
      "level": 2,
      "body": "Choose values close to and far from the boundary."
    },
    {
      "level": 3,
      "body": "Keep the loop assertion explicit.",
      "solutionCode": "#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct Percentage(u8);\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum PercentageError {\n    OutOfRange,\n}\n\nimpl Percentage {\n    pub fn value(self) -> u8 {\n        self.0\n    }\n}\n\nimpl TryFrom<u16> for Percentage {\n    type Error = PercentageError;\n\n    fn try_from(value: u16) -> Result<Self, Self::Error> {\n        if value > 100 {\n            return Err(PercentageError::OutOfRange);\n        }\n\n        Ok(Self(value as u8))\n    }\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n\n    #[test]\n    fn accepts_valid_percentages() {\n        let cases = [(0, 0), (50, 50), (100, 100)];\n\n        for (input, expected) in cases {\n            assert_eq!(\n                Percentage::try_from(input).map(Percentage::value),\n                Ok(expected)\n            );\n        }\n    }\n\n    #[test]\n    fn rejects_invalid_percentages() {\n        for input in [101, 150, 1_000] {\n            assert_eq!(Percentage::try_from(input), Err(PercentageError::OutOfRange));\n        }\n    }\n}"
    }
  ],
  "completionExplanation": "The example suite now documents both accepted and rejected input partitions.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "source_includes",
            "requiredSnippets": [
              "fn rejects_invalid_percentages()",
              "[101, 150, 1_000]"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
