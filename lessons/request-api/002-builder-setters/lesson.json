{
  "schemaVersion": 2,
  "id": "request-builder-owned-setters-002",
  "arcId": "request-api",
  "arcTitle": "Build a request API",
  "order": 10,
  "day": 2,
  "arcLength": 6,
  "title": "Add consuming builder setters",
  "conceptId": "builder-setters",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "A request builder should support chained calls without exposing all its intermediate fields.",
  "instructions": "Keep the Request type, then add RequestBuilder with optional method and path fields. Implement RequestBuilder::new() -> Self and RequestBuilder::method(mut self, method: impl Into<String>) -> Self.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Taking mut self works well for chainable builder setters."
    },
    {
      "level": 2,
      "body": "impl Into<String> accepts both borrowed and owned string inputs."
    },
    {
      "level": 3,
      "body": "Return self after updating the field.",
      "solutionCode": "#[derive(Debug, Clone, PartialEq, Eq)]\npub struct Request {\n    pub method: String,\n    pub path: String,\n    pub body: Option<String>,\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct RequestBuilder {\n    method: Option<String>,\n    path: Option<String>,\n}\n\nimpl RequestBuilder {\n    pub fn new() -> Self {\n        Self {\n            method: None,\n            path: None,\n        }\n    }\n\n    pub fn method(mut self, method: impl Into<String>) -> Self {\n        self.method = Some(method.into());\n        self\n    }\n}"
    }
  ],
  "completionExplanation": "Consuming setters make the builder ergonomic while preserving ownership. Each call returns the next builder state for the chain.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "impl_method",
            "implFor": "RequestBuilder",
            "methodName": "method",
            "requiredSignatureIncludes": [
              "mut self",
              "impl Into<String>",
              "Self"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
