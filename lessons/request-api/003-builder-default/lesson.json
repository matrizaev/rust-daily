{
  "schemaVersion": 2,
  "id": "request-builder-default-003",
  "arcId": "request-api",
  "arcTitle": "Build a request API",
  "order": 15,
  "day": 3,
  "arcLength": 6,
  "title": "Start the builder from Default",
  "conceptId": "builder-default",
  "difficulty": "easy",
  "estimatedMinutes": 6,
  "scenario": "Callers should be able to create an empty builder using the standard Default trait.",
  "instructions": "Derive Default for RequestBuilder. Option fields already default to None, so the empty builder does not need a manual impl.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Default should produce the same empty state every time."
    },
    {
      "level": 2,
      "body": "Option<T> implements Default by returning None."
    },
    {
      "level": 3,
      "body": "Add Default to the derive list on RequestBuilder.",
      "solutionCode": "#[derive(Debug, Clone, PartialEq, Eq)]\npub struct Request {\n    pub method: String,\n    pub path: String,\n    pub body: Option<String>,\n}\n\n#[derive(Debug, Clone, Default, PartialEq, Eq)]\npub struct RequestBuilder {\n    method: Option<String>,\n    path: Option<String>,\n}\n\nimpl RequestBuilder {\n    pub fn new() -> Self {\n        Self::default()\n    }\n\n    pub fn method(mut self, method: impl Into<String>) -> Self {\n        self.method = Some(method.into());\n        self\n    }\n}"
    }
  ],
  "completionExplanation": "Deriving Default is idiomatic when every field already has the desired default. Option fields make the empty builder state explicit by starting as None.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "derived_trait_for_type",
            "traitName": "Default",
            "typeName": "RequestBuilder"
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
