{
  "schemaVersion": 2,
  "id": "currency-enum-002",
  "arcId": "money-value-object",
  "arcTitle": "Money and currency domain representation",
  "order": 38,
  "day": 2,
  "arcLength": 6,
  "title": "Add supported Currency variants",
  "conceptId": "currency-enum",
  "difficulty": "easy",
  "estimatedMinutes": 5,
  "scenario": "The payment gateway is expanding to GBP, and adapter code needs a stable ISO currency code without matching on raw strings in every caller.",
  "instructions": "Add Gbp to the Currency enum and implement Currency::code(self) -> &'static str for USD, EUR, and GBP.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Enums are updated by adding new variants separated by commas."
    },
    {
      "level": 2,
      "body": "Add Gbp as a variant to the Currency enum, then match on self in code."
    },
    {
      "level": 3,
      "body": "A canonical solution adds Gbp and centralizes the ISO text mapping.",
      "solutionCode": "#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum Currency {\n    Usd,\n    Eur,\n    Gbp,\n}\n\nimpl Currency {\n    pub fn code(self) -> &'static str {\n        match self {\n            Self::Usd => \"USD\",\n            Self::Eur => \"EUR\",\n            Self::Gbp => \"GBP\",\n        }\n    }\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct Money {\n    amount: u64,\n    currency: Currency,\n}\n"
    }
  ],
  "completionExplanation": "Updating the enum and code method extends the supported domain concept while keeping external ISO text mapping centralized.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "enum_unit_variants",
            "enumName": "Currency",
            "requiredVariants": [
              "Usd",
              "Eur",
              "Gbp"
            ]
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "pub fn code",
              "\"USD\"",
              "\"EUR\"",
              "\"GBP\""
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      },
      {
        "mode": "backend-compile-fail",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "cases": [
          {
            "name": "money-direct-fields",
            "expectedDiagnostics": [
              "private"
            ],
            "forbiddenDiagnostics": [
              "has no field named"
            ],
            "sourcePath": "compile_fail/money_direct_fields.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
