{
  "schemaVersion": 2,
  "id": "register-usecase-003",
  "arcId": "register-user-use-case",
  "arcTitle": "Register user use case boundaries",
  "order": 75,
  "day": 3,
  "arcLength": 6,
  "title": "Implement a use case over a port",
  "conceptId": "register-usecase-function",
  "difficulty": "medium",
  "estimatedMinutes": 9,
  "scenario": "A use case coordinates domain rules and ports. It should be generic over the port trait so infrastructure stays replaceable.",
  "instructions": "Implement register_user over UserRepository. Return DuplicateEmail if the email already exists; otherwise save the new user.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Keep the Register user use case boundaries code active and implement register_user over UserRepository. Return DuplicateEmail if the email already exists; otherwise save the new user."
    },
    {
      "level": 2,
      "body": "Use the Rust API named in this lesson instead of adding a side helper; the previous arc code should stay in the main solution."
    },
    {
      "level": 3,
      "body": "One complete solution for Implement a use case over a port is shown below.",
      "solutionCode": "pub mod domain {\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct EmailAddress(pub String);\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct UserId(pub u64);\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct RegisterUserCommand { pub email: EmailAddress, pub display_name: String }\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct NewUser { pub email: EmailAddress, pub display_name: String }\n}\n\npub mod application {\n    use super::domain::{NewUser, RegisterUserCommand, UserId};\n    use super::domain::EmailAddress;\n\n    #[derive(Debug, Clone, Copy, PartialEq, Eq)]\n    pub enum RepositoryError { Unavailable }\n    #[derive(Debug, Clone, Copy, PartialEq, Eq)]\n    pub enum RegisterUserError { DuplicateEmail, Repository(RepositoryError) }\n\n    pub trait UserRepository {\n        fn exists_by_email(&self, email: &EmailAddress) -> Result<bool, RepositoryError>;\n        fn save(&mut self, user: NewUser) -> Result<UserId, RepositoryError>;\n    }\n\n    pub fn register_user<R: UserRepository>(repo: &mut R, command: RegisterUserCommand) -> Result<UserId, RegisterUserError> {\n        if repo.exists_by_email(&command.email).map_err(RegisterUserError::Repository)? {\n            return Err(RegisterUserError::DuplicateEmail);\n        }\n\n        repo.save(NewUser { email: command.email, display_name: command.display_name })\n            .map_err(RegisterUserError::Repository)\n    }\n}"
    }
  ],
  "completionExplanation": "Implement a use case over a port now extends the active Register user use case boundaries code with register usecase function. The previous lesson's behavior remains in the main solution rather than an archived module.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "function_signature",
            "functionName": "register_user",
            "requiredSignatureIncludes": [
              "UserRepository",
              "Result<UserId, RegisterUserError>"
            ]
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "DuplicateEmail",
              "map_err(RegisterUserError::Repository)"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
