{
  "schemaVersion": 2,
  "id": "register-inmemory-repo-005",
  "arcId": "register-user-use-case",
  "arcTitle": "Register user use case boundaries",
  "order": 77,
  "day": 5,
  "arcLength": 6,
  "title": "Share an in-memory repository safely",
  "conceptId": "register-inmemory-repo",
  "difficulty": "medium",
  "estimatedMinutes": 9,
  "scenario": "Infrastructure implements application ports. The application trait stays the dependency boundary.",
  "instructions": "In src/infrastructure.rs add InMemoryUserRepository backed by Arc<tokio::sync::RwLock<_>>. Implement UserRepository with read locking for lookups and write locking for atomic duplicate checking and insertion.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "readonly",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "src/domain.rs",
      "role": "readonly",
      "sourcePath": "starter/src/domain.rs"
    },
    {
      "path": "src/application.rs",
      "role": "readonly",
      "sourcePath": "starter/src/application.rs"
    },
    {
      "path": "src/adapters.rs",
      "role": "readonly",
      "sourcePath": "starter/src/adapters.rs"
    },
    {
      "path": "src/infrastructure.rs",
      "role": "editable",
      "sourcePath": "starter/src/infrastructure.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Arc allows cheap repository clones to share one state."
    },
    {
      "level": 2,
      "body": "Use a read lock for email_exists."
    },
    {
      "level": 3,
      "body": "Repeat duplicate detection under the write lock so concurrent saves stay atomic.",
      "solutionCode": "use std::{collections::HashMap, sync::Arc};\n\nuse tokio::sync::RwLock;\n\nuse crate::application::{NewUser, RepositoryError, UserId, UserRepository};\n\n#[derive(Debug, Clone)]\npub struct InMemoryUserRepository {\n    state: Arc<RwLock<State>>,\n}\n\n#[derive(Debug)]\nstruct State {\n    next_id: u64,\n    users_by_email: HashMap<String, UserId>,\n}\n\nimpl InMemoryUserRepository {\n    pub fn new() -> Self {\n        Self {\n            state: Arc::new(RwLock::new(State {\n                next_id: 1,\n                users_by_email: HashMap::new(),\n            })),\n        }\n    }\n\n    pub async fn len(&self) -> usize {\n        self.state.read().await.users_by_email.len()\n    }\n}\n\nimpl Default for InMemoryUserRepository {\n    fn default() -> Self {\n        Self::new()\n    }\n}\n\nimpl UserRepository for InMemoryUserRepository {\n    async fn email_exists(&self, email: &str) -> Result<bool, RepositoryError> {\n        Ok(self.state.read().await.users_by_email.contains_key(email))\n    }\n\n    async fn save(&self, user: NewUser) -> Result<UserId, RepositoryError> {\n        let mut state = self.state.write().await;\n\n        if state.users_by_email.contains_key(user.email()) {\n            return Err(RepositoryError::Conflict);\n        }\n\n        let user_id = UserId::new(state.next_id);\n        state.next_id += 1;\n        state\n            .users_by_email\n            .insert(user.email().to_owned(), user_id);\n\n        Ok(user_id)\n    }\n}"
    }
  ],
  "completionExplanation": "The infrastructure adapter supports concurrent async access without leaking synchronization primitives through the repository port.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "source_includes",
            "requiredSnippets": [
              "Arc<RwLock<State>>",
              "self.state.read().await",
              "self.state.write().await"
            ]
          },
          {
            "type": "impl_trait_for_type",
            "traitName": "UserRepository",
            "typeName": "InMemoryUserRepository"
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "advanced",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
