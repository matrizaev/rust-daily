{
  "schemaVersion": 2,
  "id": "register-handler-boundary-006",
  "arcId": "register-user-use-case",
  "arcTitle": "Register user use case boundaries",
  "order": 78,
  "day": 6,
  "arcLength": 6,
  "title": "Add an Actix handler boundary",
  "conceptId": "register-handler-boundary",
  "difficulty": "medium",
  "estimatedMinutes": 10,
  "scenario": "A handler adapter should translate request DTOs, call timeout-aware application code, and return response data without pushing HTTP concepts into domain code.",
  "instructions": "The readonly application module already exposes register_user_with_timeout and TimedOut. In src/adapters.rs add Response, handle_register_user, and a thin Actix register_user_handler using web::Data, web::Json, HttpResponse, and Responder. Keep HTTP status mapping in adapters and the use case free of Actix types.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "readonly",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "src/domain.rs",
      "role": "readonly",
      "sourcePath": "starter/src/domain.rs"
    },
    {
      "path": "src/application.rs",
      "role": "readonly",
      "sourcePath": "starter/src/application.rs"
    },
    {
      "path": "src/infrastructure.rs",
      "role": "readonly",
      "sourcePath": "starter/src/infrastructure.rs"
    },
    {
      "path": "src/adapters.rs",
      "role": "editable",
      "sourcePath": "starter/src/adapters.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Timeout is application policy; this file should translate its result into adapter responses."
    },
    {
      "level": 2,
      "body": "The handler should only extract inputs, call adapter/application code, and build an HTTP response."
    },
    {
      "level": 3,
      "body": "Do not pass web::Json or HttpResponse into domain or application modules.",
      "solutionCode": "use std::time::Duration;\n\nuse actix_web::{HttpResponse, Responder, http::StatusCode, web};\nuse serde::Deserialize;\nuse thiserror::Error;\n\nuse crate::{\n    application::{RegisterUserError, UserRepository, register_user_with_timeout},\n    domain::{EmailAddress, RegisterUserCommand},\n};\n\n#[derive(Debug, Clone, Deserialize, PartialEq, Eq)]\npub struct RegisterUserRequest {\n    pub email: String,\n    pub display_name: String,\n}\n\n#[derive(Debug, Clone, Copy, Error, PartialEq, Eq)]\npub enum RequestError {\n    #[error(\"email is empty\")]\n    EmptyEmail,\n    #[error(\"display name is empty\")]\n    EmptyDisplayName,\n}\n\nimpl TryFrom<RegisterUserRequest> for RegisterUserCommand {\n    type Error = RequestError;\n\n    fn try_from(request: RegisterUserRequest) -> Result<Self, Self::Error> {\n        let email = request.email.trim();\n        let display_name = request.display_name.trim();\n\n        if email.is_empty() {\n            return Err(RequestError::EmptyEmail);\n        }\n\n        if display_name.is_empty() {\n            return Err(RequestError::EmptyDisplayName);\n        }\n\n        Ok(RegisterUserCommand::new(\n            EmailAddress::new(email),\n            display_name,\n        ))\n    }\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct Response {\n    pub status: u16,\n    pub body: String,\n}\n\npub async fn handle_register_user<R: UserRepository>(\n    repository: &R,\n    request: RegisterUserRequest,\n) -> Response {\n    let command = match RegisterUserCommand::try_from(request) {\n        Ok(command) => command,\n        Err(error) => {\n            return Response {\n                status: 400,\n                body: error.to_string(),\n            };\n        }\n    };\n\n    match register_user_with_timeout(repository, command, Duration::from_secs(1)).await {\n        Ok(user_id) => Response {\n            status: 201,\n            body: user_id.value().to_string(),\n        },\n        Err(RegisterUserError::DuplicateEmail) => Response {\n            status: 409,\n            body: \"email already exists\".to_owned(),\n        },\n        Err(RegisterUserError::TimedOut) => Response {\n            status: 504,\n            body: \"registration timed out\".to_owned(),\n        },\n        Err(RegisterUserError::Repository(_)) => Response {\n            status: 503,\n            body: \"repository is unavailable\".to_owned(),\n        },\n    }\n}\n\npub async fn register_user_handler<R>(\n    repository: web::Data<R>,\n    request: web::Json<RegisterUserRequest>,\n) -> impl Responder\nwhere\n    R: UserRepository + 'static,\n{\n    let response = handle_register_user(repository.get_ref(), request.into_inner()).await;\n    let status = StatusCode::from_u16(response.status).unwrap_or(StatusCode::INTERNAL_SERVER_ERROR);\n\n    HttpResponse::build(status).body(response.body)\n}"
    }
  ],
  "completionExplanation": "The arc ends with cancellation-aware async application code and a thin Actix boundary that preserves module ownership.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "source_includes",
            "requiredSnippets": [
              "use actix_web::",
              "HttpResponse",
              "Responder",
              "pub async fn register_user_handler",
              "RegisterUserError::TimedOut"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "advanced",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
