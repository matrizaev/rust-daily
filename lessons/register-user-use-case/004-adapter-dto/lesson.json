{
  "schemaVersion": 2,
  "id": "register-adapter-dto-004",
  "arcId": "register-user-use-case",
  "arcTitle": "Register user use case boundaries",
  "order": 76,
  "day": 4,
  "arcLength": 6,
  "title": "Deserialize adapter input at the edge",
  "conceptId": "register-adapter-dto",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "Adapter DTOs should be converted into domain commands before use-case logic runs.",
  "instructions": "In src/adapters.rs add serde-deserializable RegisterUserRequest and typed RequestError. Implement TryFrom<RegisterUserRequest> for RegisterUserCommand, trimming and validating input before it enters the use case.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "readonly",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "src/domain.rs",
      "role": "readonly",
      "sourcePath": "starter/src/domain.rs"
    },
    {
      "path": "src/application.rs",
      "role": "readonly",
      "sourcePath": "starter/src/application.rs"
    },
    {
      "path": "src/adapters.rs",
      "role": "editable",
      "sourcePath": "starter/src/adapters.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Serde belongs on the adapter DTO, not on the command."
    },
    {
      "level": 2,
      "body": "Trim before constructing domain values."
    },
    {
      "level": 3,
      "body": "Return RequestError before calling application code.",
      "solutionCode": "use serde::Deserialize;\nuse thiserror::Error;\n\nuse crate::domain::{EmailAddress, RegisterUserCommand};\n\n#[derive(Debug, Clone, Deserialize, PartialEq, Eq)]\npub struct RegisterUserRequest {\n    pub email: String,\n    pub display_name: String,\n}\n\n#[derive(Debug, Clone, Copy, Error, PartialEq, Eq)]\npub enum RequestError {\n    #[error(\"email is empty\")]\n    EmptyEmail,\n    #[error(\"display name is empty\")]\n    EmptyDisplayName,\n}\n\nimpl TryFrom<RegisterUserRequest> for RegisterUserCommand {\n    type Error = RequestError;\n\n    fn try_from(request: RegisterUserRequest) -> Result<Self, Self::Error> {\n        let email = request.email.trim();\n        let display_name = request.display_name.trim();\n\n        if email.is_empty() {\n            return Err(RequestError::EmptyEmail);\n        }\n\n        if display_name.is_empty() {\n            return Err(RequestError::EmptyDisplayName);\n        }\n\n        Ok(RegisterUserCommand::new(\n            EmailAddress::new(email),\n            display_name,\n        ))\n    }\n}"
    }
  ],
  "completionExplanation": "JSON-shaped input is converted into framework-free domain data at the adapter edge.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "derived_trait_for_type",
            "traitName": "serde::Deserialize",
            "typeName": "RegisterUserRequest"
          },
          {
            "type": "impl_trait_for_type",
            "traitName": "TryFrom<RegisterUserRequest>",
            "typeName": "RegisterUserCommand"
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "advanced",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
