{
  "schemaVersion": 2,
  "id": "register-domain-command-001",
  "arcId": "register-user-use-case",
  "arcTitle": "Register user use case boundaries",
  "order": 73,
  "day": 1,
  "arcLength": 6,
  "title": "Keep async service types out of the domain",
  "conceptId": "register-domain-command",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "Domain types should not depend on HTTP, JSON, databases, or logging setup. Start with pure command and value-object types.",
  "instructions": "Define a domain module with EmailAddress and RegisterUserCommand. Keep fields private, expose borrowed accessors, and do not import Tokio, Actix, or serde into the domain module.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "The domain should not know which runtime or web framework calls it."
    },
    {
      "level": 2,
      "body": "Use private owned fields and borrowed accessors."
    },
    {
      "level": 3,
      "body": "Keep all imports out of the domain module.",
      "solutionCode": "pub mod domain {\n    #[derive(Debug, Clone, PartialEq, Eq, Hash)]\n    pub struct EmailAddress(String);\n\n    impl EmailAddress {\n        pub fn new(value: impl Into<String>) -> Self {\n            Self(value.into())\n        }\n\n        pub fn as_str(&self) -> &str {\n            &self.0\n        }\n    }\n\n    #[derive(Debug, Clone, PartialEq, Eq)]\n    pub struct RegisterUserCommand {\n        email: EmailAddress,\n        display_name: String,\n    }\n\n    impl RegisterUserCommand {\n        pub fn new(email: EmailAddress, display_name: impl Into<String>) -> Self {\n            Self {\n                email,\n                display_name: display_name.into(),\n            }\n        }\n\n        pub fn email(&self) -> &EmailAddress {\n            &self.email\n        }\n\n        pub fn display_name(&self) -> &str {\n            &self.display_name\n        }\n    }\n}"
    }
  ],
  "completionExplanation": "The core register-user types are runtime-independent and can be reused by HTTP, jobs, or tests.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "tuple_struct_fields",
            "structName": "EmailAddress",
            "requiredTypes": [
              "String"
            ]
          },
          {
            "type": "impl_method",
            "implFor": "RegisterUserCommand",
            "methodName": "email",
            "requiredSignatureIncludes": [
              "fn email(&self) -> &EmailAddress"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "advanced",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
