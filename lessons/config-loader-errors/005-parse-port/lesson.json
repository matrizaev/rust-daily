{
  "schemaVersion": 2,
  "id": "config-err-parse-port-005",
  "arcId": "config-loader-errors",
  "arcTitle": "Hierarchical configuration loader errors",
  "order": 65,
  "day": 5,
  "arcLength": 6,
  "title": "Wrap ParseIntError for port parsing",
  "conceptId": "config-err-parse-wrap",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "Parsing configuration values should keep lower-level parse details while returning the loader error type.",
  "instructions": "Add InvalidPort(ParseIntError), implement From<ParseIntError>, and use ? in parse_port.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Focus on the one public API shape requested by the lesson."
    },
    {
      "level": 2,
      "body": "Prefer standard traits, typed errors, borrowed views, and explicit boundary conversions."
    },
    {
      "level": 3,
      "body": "A canonical idiomatic solution is shown below.",
      "solutionCode": "use std::num::ParseIntError;\n\n#[derive(Debug)]\npub enum ConfigLoadError {\n    InvalidPort(ParseIntError),\n}\n\nimpl From<ParseIntError> for ConfigLoadError {\n    fn from(error: ParseIntError) -> Self {\n        ConfigLoadError::InvalidPort(error)\n    }\n}\n\npub fn parse_port(value: &str) -> Result<u16, ConfigLoadError> {\n    let port = value.parse::<u16>()?;\n\n    Ok(port)\n}"
    }
  ],
  "completionExplanation": "This lesson keeps config-err-parse-wrap explicit in the type system and checks public behavior without relying on panics or unchecked shortcuts.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "impl_trait_for_type",
            "traitName": "From<ParseIntError>",
            "typeName": "ConfigLoadError"
          },
          {
            "type": "function_signature",
            "functionName": "parse_port",
            "requiredSignatureIncludes": [
              "Result<u16, ConfigLoadError>"
            ]
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "?"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
