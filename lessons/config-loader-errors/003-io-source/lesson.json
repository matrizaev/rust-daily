{
  "schemaVersion": 2,
  "id": "config-err-io-source-003",
  "arcId": "config-loader-errors",
  "arcTitle": "Hierarchical configuration loader errors",
  "order": 63,
  "day": 3,
  "arcLength": 6,
  "title": "Preserve an I/O error as a source",
  "conceptId": "config-err-io-wrap",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "When file reading fails, the high-level config error should keep the original std::io::Error as its source.",
  "instructions": "Change FileRead into a variant that stores std::io::Error in a source field marked #[source]. Update kind without changing the public error message.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "thiserror can expose a source without a handwritten Error implementation."
    },
    {
      "level": 2,
      "body": "Use a named source field while this lesson focuses on explicit source tracking."
    },
    {
      "level": 3,
      "body": "Match FileRead { .. } in kind.",
      "solutionCode": "use thiserror::Error;\n\n#[derive(Debug, Error)]\npub enum ConfigLoadError {\n    #[error(\"missing APP_PORT\")]\n    MissingEnvironment,\n    #[error(\"invalid APP_PORT\")]\n    InvalidPort,\n    #[error(\"failed to read config file\")]\n    FileRead {\n        #[source]\n        source: std::io::Error,\n    },\n}\n\nimpl ConfigLoadError {\n    pub fn kind(&self) -> &'static str {\n        match self {\n            Self::MissingEnvironment => \"missing_environment\",\n            Self::InvalidPort => \"invalid_port\",\n            Self::FileRead { .. } => \"file_read\",\n        }\n    }\n}"
    }
  ],
  "completionExplanation": "The top-level config error keeps its stable message while preserving the concrete I/O cause in the standard error chain.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "enum_unit_variants",
            "enumName": "ConfigLoadError",
            "requiredVariants": [
              "FileRead"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "advanced",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
