{
  "schemaVersion": 2,
  "id": "inventory-sort-by-key-006",
  "arcId": "inventory-summary",
  "arcTitle": "Summarize inventory",
  "order": 27,
  "day": 6,
  "arcLength": 6,
  "title": "Sort items by borrowed comparison",
  "conceptId": "sort-by-key",
  "difficulty": "easy",
  "estimatedMinutes": 5,
  "scenario": "Before rendering an inventory table, the service wants stable alphabetical ordering by item name without allocating temporary sort keys.",
  "instructions": "Implement sort_by_name without cloning item names. Sort the mutable slice by comparing each item's borrowed name.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Slices expose in-place sorting methods."
    },
    {
      "level": 2,
      "body": "For String fields, sort_by with name.cmp(&other.name) avoids allocating cloned keys."
    },
    {
      "level": 3,
      "body": "The comparison closure receives two item references: left and right.",
      "solutionCode": "pub struct Item {\n    pub sku: String,\n    pub name: String,\n    pub quantity: u32,\n    pub reserved: u32,\n}\n\npub struct Inventory {\n    items: Vec<Item>,\n}\n\nimpl Inventory {\n    pub fn new(items: Vec<Item>) -> Self {\n        Self { items }\n    }\n}\n\npub fn sort_by_name(items: &mut [Item]) {\n    items.sort_by(|left, right| left.name.cmp(&right.name));\n}\n\n\npub fn available_names(items: &[Item]) -> Vec<&str> {\n    items\n        .iter()\n        .filter_map(|item| {\n            if item.quantity > item.reserved {\n                Some(item.name.as_str())\n            } else {\n                None\n            }\n        })\n        .collect()\n}\n\n\npub fn total_quantity(items: &[Item]) -> u32 {\n    items.iter().fold(0, |total, item| total + item.quantity)\n}\n\n\npub fn reorder_notes(notes: &[String]) -> Vec<String> {\n    let mut relevant_notes = Vec::new();\n\n    for note in notes {\n        let normalized = note.trim().to_lowercase();\n        if normalized.is_empty() {\n            continue;\n        }\n\n        let is_inventory_note =\n            normalized.contains(\"urgent\") || normalized.contains(\"stock\");\n        if is_inventory_note {\n            relevant_notes.push(normalized);\n        }\n    }\n\n    relevant_notes\n}\n\n\nimpl IntoIterator for Inventory {\n    type Item = Item;\n    type IntoIter = std::vec::IntoIter<Item>;\n\n    fn into_iter(self) -> Self::IntoIter {\n        self.items.into_iter()\n    }\n}"
    }
  ],
  "completionExplanation": "Sorting by borrowed comparison keeps the ordering rule close to the sort call without cloning every String. That is usually the better API choice when the key is already stored in each item.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "function_signature",
            "functionName": "sort_by_name",
            "requiredSignatureIncludes": [
              "&mut [Item]"
            ]
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "sort_by",
              ".cmp(&"
            ],
            "forbiddenSnippets": [
              "clone",
              "to_owned",
              "to_string",
              "sort_by_key"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
