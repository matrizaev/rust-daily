{
  "schemaVersion": 2,
  "id": "register-command-value-002",
  "arcId": "dto-conversions",
  "arcTitle": "Converting DTOs into domain commands",
  "order": 50,
  "day": 2,
  "arcLength": 6,
  "title": "Keep the validated command serde-free",
  "conceptId": "register-command-value",
  "difficulty": "easy",
  "estimatedMinutes": 6,
  "scenario": "The application layer should receive a command type, not raw optional DTO fields. Keep command fields private and expose read-only accessors.",
  "instructions": "Keep RegisterUserDto active. Add RegisterUserCommand with private email and display_name fields, a constructor, and borrowed accessors. Do not derive serde traits on the command.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "DTOs and commands serve different layers."
    },
    {
      "level": 2,
      "body": "The command owns validated data but exposes it through borrowed accessors."
    },
    {
      "level": 3,
      "body": "Keep Deserialize on RegisterUserDto only.",
      "solutionCode": "use serde::Deserialize;\n\n#[derive(Debug, Clone, Deserialize, PartialEq, Eq)]\npub struct RegisterUserDto {\n    pub email: Option<String>,\n    pub display_name: Option<String>,\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct RegisterUserCommand {\n    email: String,\n    display_name: String,\n}\n\nimpl RegisterUserCommand {\n    pub fn new(email: impl Into<String>, display_name: impl Into<String>) -> Self {\n        Self {\n            email: email.into(),\n            display_name: display_name.into(),\n        }\n    }\n\n    pub fn email(&self) -> &str {\n        &self.email\n    }\n\n    pub fn display_name(&self) -> &str {\n        &self.display_name\n    }\n}"
    }
  ],
  "completionExplanation": "The DTO stays a boundary shape, while the command is a domain/application shape with private state and a narrow API.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "struct_fields",
            "structName": "RegisterUserCommand",
            "requiredFields": [
              {
                "name": "email",
                "typeIncludes": [
                  "String"
                ]
              },
              {
                "name": "display_name",
                "typeIncludes": [
                  "String"
                ]
              }
            ]
          },
          {
            "type": "impl_method",
            "implFor": "RegisterUserCommand",
            "methodName": "email",
            "requiredSignatureIncludes": [
              "fn email(&self) -> &str"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "advanced",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      },
      {
        "mode": "backend-compile-fail",
        "timeoutMs": 10000,
        "dependencySet": "advanced",
        "cases": [
          {
            "name": "command-direct-fields",
            "expectedDiagnostics": [
              "private"
            ],
            "forbiddenDiagnostics": [
              "has no field named"
            ],
            "sourcePath": "compile_fail/command_direct_fields.rs"
          },
          {
            "name": "command-serde-free",
            "expectedDiagnostics": [
              "Serialize"
            ],
            "sourcePath": "compile_fail/command_serde_free.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
