{
  "schemaVersion": 2,
  "id": "boundary-status-code-006",
  "arcId": "boundary-error-mapping",
  "arcTitle": "Translating errors across boundaries",
  "order": 72,
  "day": 6,
  "arcLength": 6,
  "title": "Map typed application errors at the adapter edge",
  "conceptId": "boundary-http-status",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "HTTP status codes belong at the adapter edge. Translate typed application errors there instead of putting HTTP concepts in domain code.",
  "instructions": "Implement status_code(&CreateOrderUseCaseError) -> u16 at the adapter boundary: 400 for domain errors, 409 for repository conflicts, and 503 for repository unavailability.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "HTTP status is adapter policy, not a property of the domain error."
    },
    {
      "level": 2,
      "body": "Match explicitly on typed variants."
    },
    {
      "level": 3,
      "body": "Keep the mapping simple and exhaustive inside the crate.",
      "solutionCode": "use thiserror::Error;\n\n#[non_exhaustive]\n#[derive(Debug, Clone, Copy, Error, PartialEq, Eq)]\npub enum CreateOrderError {\n    #[error(\"order must contain at least one line\")]\n    EmptyOrder,\n    #[error(\"order line quantity must be positive\")]\n    InvalidQuantity,\n}\n\n#[non_exhaustive]\n#[derive(Debug, Clone, Copy, Error, PartialEq, Eq)]\npub enum RepositoryError {\n    #[error(\"repository is unavailable\")]\n    Unavailable,\n    #[error(\"order conflicts with existing data\")]\n    Conflict,\n}\n\n#[non_exhaustive]\n#[derive(Debug, Clone, Copy, Error, PartialEq, Eq)]\npub enum CreateOrderUseCaseError {\n    #[error(transparent)]\n    Domain(#[from] CreateOrderError),\n    #[error(transparent)]\n    Repository(#[from] RepositoryError),\n}\n\nimpl CreateOrderUseCaseError {\n    pub fn is_retryable(&self) -> bool {\n        matches!(self, Self::Repository(RepositoryError::Unavailable))\n    }\n}\n\npub fn status_code(error: &CreateOrderUseCaseError) -> u16 {\n    match error {\n        CreateOrderUseCaseError::Domain(_) => 400,\n        CreateOrderUseCaseError::Repository(RepositoryError::Conflict) => 409,\n        CreateOrderUseCaseError::Repository(RepositoryError::Unavailable) => 503,\n    }\n}"
    }
  ],
  "completionExplanation": "The adapter translates application failures into HTTP policy without leaking status codes into domain or repository types.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "function_signature",
            "functionName": "status_code",
            "requiredSignatureIncludes": [
              "fn status_code(error: &CreateOrderUseCaseError) -> u16"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "advanced",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
