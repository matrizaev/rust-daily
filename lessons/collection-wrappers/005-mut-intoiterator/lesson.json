{
  "schemaVersion": 2,
  "id": "wrap-mut-intoiterator-005",
  "arcId": "collection-wrappers",
  "arcTitle": "Exposing iteration on collection wrappers",
  "order": 59,
  "day": 5,
  "arcLength": 6,
  "title": "Iterate mutably over collection wrappers",
  "conceptId": "wrap-mut-into-iterator",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "scenario": "Some update operations should borrow the wrapper mutably and edit each line in place without exposing the Vec field.",
  "instructions": "The starter already supports owned and shared borrowed iteration. Implement IntoIterator for &mut OrderLines, yielding &mut OrderLine values.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Implement the trait for &'a mut OrderLines with Item = &'a mut OrderLine."
    },
    {
      "level": 2,
      "body": "Return self.lines.iter_mut() so callers can update each line in place without exposing the Vec field."
    },
    {
      "level": 3,
      "body": "One complete solution for Iterate mutably over collection wrappers is shown below.",
      "solutionCode": "#[derive(Debug, Clone, PartialEq, Eq)]\npub struct OrderLine {\n    pub sku: String,\n    pub quantity: u32,\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct OrderLines {\n    lines: Vec<OrderLine>,\n}\n\nimpl OrderLines {\n    pub fn new(lines: Vec<OrderLine>) -> Self {\n        Self { lines }\n    }\n\n    pub fn len(&self) -> usize {\n        self.lines.len()\n    }\n\n    pub fn is_empty(&self) -> bool {\n        self.lines.is_empty()\n    }\n\n    pub fn iter(&self) -> std::slice::Iter<'_, OrderLine> {\n        self.lines.iter()\n    }\n}\n\nimpl IntoIterator for OrderLines {\n    type Item = OrderLine;\n    type IntoIter = std::vec::IntoIter<OrderLine>;\n\n    fn into_iter(self) -> Self::IntoIter {\n        self.lines.into_iter()\n    }\n}\n\nimpl<'a> IntoIterator for &'a OrderLines {\n    type Item = &'a OrderLine;\n    type IntoIter = std::slice::Iter<'a, OrderLine>;\n\n    fn into_iter(self) -> Self::IntoIter {\n        self.iter()\n    }\n}\n\nimpl<'a> IntoIterator for &'a mut OrderLines {\n    type Item = &'a mut OrderLine;\n    type IntoIter = std::slice::IterMut<'a, OrderLine>;\n\n    fn into_iter(self) -> Self::IntoIter {\n        self.lines.iter_mut()\n    }\n}"
    }
  ],
  "completionExplanation": "Mutable IntoIterator enables `for line in &mut order_lines` while keeping the collection field private.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "source_includes",
            "requiredSnippets": [
              "impl<'a> IntoIterator for &'a mut OrderLines",
              "type Item = &'a mut OrderLine",
              "iter_mut"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
