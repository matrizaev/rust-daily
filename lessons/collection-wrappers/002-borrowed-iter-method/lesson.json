{
  "schemaVersion": 2,
  "id": "wrap-borrowed-iter-002",
  "arcId": "collection-wrappers",
  "arcTitle": "Exposing iteration on collection wrappers",
  "order": 56,
  "day": 2,
  "arcLength": 6,
  "title": "Expose borrowed iteration with iter",
  "conceptId": "wrap-borrowed-iter",
  "difficulty": "easy",
  "estimatedMinutes": 6,
  "scenario": "Callers often need to inspect a collection wrapper without consuming it. A borrowed iterator exposes a view without leaking fields.",
  "instructions": "The starter already has OrderLines::new, len, and is_empty. Add OrderLines::iter(&self) -> std::slice::Iter<'_, OrderLine>.",
  "files": [
    {
      "path": "src/lib.rs",
      "role": "editable",
      "sourcePath": "starter/src/lib.rs"
    },
    {
      "path": "tests/public.rs",
      "role": "test",
      "sourcePath": "tests/public.rs"
    }
  ],
  "hints": [
    {
      "level": 1,
      "body": "Add iter to the existing OrderLines impl block."
    },
    {
      "level": 2,
      "body": "Return self.lines.iter() so callers can inspect lines without consuming the wrapper."
    },
    {
      "level": 3,
      "body": "One complete solution for Expose borrowed iteration with iter is shown below.",
      "solutionCode": "#[derive(Debug, Clone, PartialEq, Eq)]\npub struct OrderLine {\n    pub sku: String,\n    pub quantity: u32,\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub struct OrderLines {\n    lines: Vec<OrderLine>,\n}\n\nimpl OrderLines {\n    pub fn new(lines: Vec<OrderLine>) -> Self {\n        Self { lines }\n    }\n\n    pub fn len(&self) -> usize {\n        self.lines.len()\n    }\n\n    pub fn is_empty(&self) -> bool {\n        self.lines.is_empty()\n    }\n\n    pub fn iter(&self) -> std::slice::Iter<'_, OrderLine> {\n        self.lines.iter()\n    }\n}"
    }
  ],
  "completionExplanation": "Borrowed iteration exposes read-only access to the wrapped lines without making the Vec field public.",
  "validation": {
    "mode": "all",
    "validations": [
      {
        "mode": "structural",
        "timeoutMs": 10000,
        "checks": [
          {
            "type": "impl_method",
            "implFor": "OrderLines",
            "methodName": "iter",
            "requiredSignatureIncludes": [
              "&self",
              "std::slice::Iter",
              "OrderLine"
            ]
          },
          {
            "type": "source_includes",
            "requiredSnippets": [
              "self.lines.iter()"
            ]
          }
        ]
      },
      {
        "mode": "backend-cargo-test",
        "timeoutMs": 10000,
        "dependencySet": "std",
        "testFiles": [
          {
            "path": "tests/public.rs",
            "sourcePath": "tests/public.rs"
          }
        ]
      }
    ]
  },
  "author": {
    "solutionPath": "solution",
    "notesPath": "notes.md"
  }
}
